/** Opciones de filas por página (API + UI) */
export const PAGE_SIZE_OPTIONS = [10, 20, 30] as const;

export const DEFAULT_PAGE_SIZE = 10;

/** Scroll interno en la tabla si hay más de N filas en la página actual */
export const SCROLL_ROW_THRESHOLD = 10;
